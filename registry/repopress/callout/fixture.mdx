import { Callout } from "@/components/repopress/callout"

# Release checklist

<Callout title="Migration note" variant="accent">
  Keep the **lockfile** in the same pull request as the installed component.
</Callout>
